# Testcase Policy

- Active testcases require explicit requirement/AC mapping.
- Each testcase row should include scope (`UI`, `API`, `Functional`, combos, or `E2E`).
- Keep `Blocked Coverage`, `Regression Impact`, and `Changelog` sections in each suite.
- Recompute Kanban TC count from active testcases only.
